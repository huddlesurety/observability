# Railway template: import this file onto the Railway canvas to create all services.
# All inter-service communication uses Railway private networking (*.railway.internal).

services:
  prometheus:
    build:
      context: ./prometheus
      dockerfile: Dockerfile
      args:
        - VERSION=${PROMETHEUS_VERSION:-v3.5.1}
    volumes:
      - prometheus_data:/prometheus
    environment:
      - OTEL_COLLECTOR_HOST=otel-collector.railway.internal
      - OTEL_COLLECTOR_PORT=8889
      - SCRAPE_INTERVAL=15s
      - EVALUATION_INTERVAL=15s
      - PORT=9090

  otel-collector:
    build:
      context: ./otel-collector
      dockerfile: Dockerfile
    environment:
      - PORT=4318
      - PROMETHEUS_EXPORTER_PORT=8889
      - HEALTH_CHECK_PORT=13133
      - TEMPO_ENDPOINT=http://tempo.railway.internal:4318
      - LOKI_ENDPOINT=http://loki.railway.internal:3100/otlp

  tempo:
    build:
      context: ./tempo
      dockerfile: Dockerfile
      args:
        - VERSION=${TEMPO_VERSION:-2.10.0}
    volumes:
      - tempo_data:/var/tempo
    environment:
      - PORT=3200
      - OTLP_HTTP_PORT=4318

  loki:
    build:
      context: ./loki
      dockerfile: Dockerfile
      args:
        - VERSION=${LOKI_VERSION:-3.6.5}
    volumes:
      - loki_data:/var/loki
    environment:
      - PORT=3100

  grafana:
    build:
      context: ./grafana
      dockerfile: Dockerfile
      args:
        - VERSION=${GRAFANA_VERSION:-12.4.0}
    volumes:
      - grafana_data:/var/lib/grafana
    environment:
      - PORT=3000
      - PROMETHEUS_HOST=prometheus.railway.internal
      - PROMETHEUS_PORT=9090
      - TEMPO_HOST=tempo.railway.internal
      - TEMPO_PORT=3200
      - LOKI_HOST=loki.railway.internal
      - LOKI_PORT=3100

volumes:
  prometheus_data:
  grafana_data:
  tempo_data:
  loki_data:
