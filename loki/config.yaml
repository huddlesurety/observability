auth_enabled: false

server:
  http_listen_port: ${PORT:-3100}
  # gRPC not needed — OTel Collector pushes via HTTP, Grafana queries via HTTP
  grpc_listen_port: 0

# Single-instance mode (inmemory ring, replication_factor=1)
common:
  path_prefix: /var/loki
  storage:
    filesystem:
      chunks_directory: /var/loki/chunks
      rules_directory: /var/loki/rules
  replication_factor: 1
  ring:
    instance_addr: 127.0.0.1
    kvstore:
      store: inmemory

limits_config:
  # Required for OTLP log ingestion
  allow_structured_metadata: true

schema_config:
  configs:
    - from: "2024-01-01"
      store: tsdb
      object_store: filesystem
      schema: v13
      index:
        prefix: index_
        period: 24h

storage_config:
  tsdb_shipper:
    active_index_directory: /var/loki/tsdb-index
    cache_location: /var/loki/tsdb-cache
