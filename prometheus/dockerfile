ARG VERSION=v3.5.1

FROM prom/prometheus:${VERSION}

COPY config.yaml /etc/prometheus/config.yaml

# Command to run Prometheus
CMD ["--config.file=/etc/prometheus/config.yaml", "--storage.tsdb.path=/prometheus"]
